Description:
    Installs the bcms_mindbody module. 

Example:
    rails generate bcms_mindbody:install

    This will:
		Copy the migrations from the gem into the project.
		Add the routes to the config/routes.rb
